class Operators {
    public static void main(String[] args) {
        // Different Operators in Java

        // arithmetic operators
        + // - (Addition)
        - // - (Subtraction)
        * // - (Multiplication)
        / // - (Division)
        % // - (Modulus)

        // increment operators
        ++a // - (Prefix)
        a++ // - (Postfix)

        // relational operators
        == // - (Double equal sign)
        != // - (Not Equal sign)
        <= // - (Less than or Equal sign)
        >= // - (Greater than or Equal sign)
        < // - (Less than sign)
        > // - (Greater than sign)
        // note: relational operators cannot use on objects

        // conditional operator
        ? // - (Ternary Operator) - operator in list comprehension

        // logical operators/ boolean operator
        ! // - (Logical NOT)
        && // - (Logical AND)
        || // - (Logical OR)
        ^ // - (Exclusive OR)

        // bitwise operators
        & // - (Bitwise AND)
        | // - (Bitwise OR)
        ~ // - (Bitwise NOT)

        // shortcut operator/ compound operator
        // += - a = a + b

        // Operator Precedence
        // The following evaluation order is used when working out an expression:
        // 1. Parentheses evaluated first.
        // 2. Incrementing and decrementing take place next.
        // 3. Multiplication, division, and modulus division occur next.
        // 4. Addition and subtraction follow.
        // 5. Comparisons take place next.
        // 6. The equal sign = is used to set a variable’s value.

    }
}
